import AuthNewPassordtemplate from '../../components/templates/authScreens/AuthNewPassordtemplate';

function AuthNewPassword({navigation}) {
    return (
        <AuthNewPassordtemplate 
        navigation={navigation}
        />
    );
}

export default AuthNewPassword;