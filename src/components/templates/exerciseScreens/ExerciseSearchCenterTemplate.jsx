import { styled } from 'styled-components/native';
import { COLORS } from '../../../constants/color';
import SearchCenterTemplate from '../homScreens/SearchCenterTemplate';

function ExerciseSearchCenterTemplate({searchCenterText}) {
    return (
        <SearchCenterTemplate searchCenterText={searchCenterText}/>
    );
}

export default ExerciseSearchCenterTemplate;