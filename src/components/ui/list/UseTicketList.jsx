import { View ,Text} from "react-native";
import { styled } from 'styled-components/native';
import { COLORS } from '../../../constants/color';

function UseTicketList(props) {
    return (
        <View>
            <Text>UseTicketList</Text>
        </View>
    );
}

export default UseTicketList;